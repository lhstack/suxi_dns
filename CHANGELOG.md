# 更新日志

## 1.0.2

**versionCode 3** · 2026-07-25

### 新功能

- **上游分组 + fallback**：上游改为按分组组织，组内并发竞速取最快，组间按顺序 fallback；组顺序可在界面长按拖拽调整
- **DNS 缓存**：进程内按「域名 + 查询类型」缓存响应，按答案区最小 TTL 过期（不缓存 SERVFAIL），命中直接返回，减少重复上游往返与射频唤醒（降低耗电）
- **手动解析**：新增菜单，输入域名做正向解析（A/AAAA/CNAME/MX/TXT/NS/SOA/SRV），输入 IP 做 PTR 反向解析；可多选类型（A/AAAA/CNAME 互斥、PTR 单独查询）、指定上游分组；不依赖 VPN 运行状态

### 变更

- 默认上游改为两组：首选组 = `223.6.6.6` / `223.5.5.5` UDP 并发，备用组 = 对应 HTTP/3 fallback
- 旧版扁平上游配置在加载时自动迁移为单个分组，不丢配置

### 说明

- 查询顺序：拦截 → 本地自定义解析 → DNS 缓存 → 上游分组解析

## 1.0.1

**versionCode 2** · 2026-07-22

### 新功能

- **自定义解析**：支持 A / AAAA / CNAME，域名 `*` 通配，可配置 TTL；查询 A/AAAA 命中 CNAME 时尽量链式补全目标记录
- **域名拦截**：通配符 / 正则匹配，命中返回 NXDOMAIN；独立可滚动规则列表（LazyColumn）
- **拦截规则批量能力**：从文件 / 自定义 URL 导入 JSON；重复覆盖、不重复追加；批量清空
- **更新默认规则**：从固定远程源同步官方 `block_rules.json`
- **首次启动初始化**（仅本地配置文件不存在时）：
  - 默认 4 个上游：UDP `223.5.5.5` / `223.6.6.6`，HTTP/3 `223.5.5.5` / `223.6.6.6`
  - 内置拦截规则（`assets/block_rules.json`，含国内外常见广告/统计域名）

### 修复

- VPN 停止后读包/写包协程异常导致闪退
- TTL 输入框无法清空（受控 `Int` 导致删不掉）

### 说明

- 查询顺序：拦截 → 本地自定义解析 → 上游并行竞速
- 已安装且本地已有配置文件的用户不会被默认规则覆盖；需要线上最新拦截列表请点「更新默认规则」

## 1.0

**versionCode 1**

- 首个可用版本：多上游并行竞速 DNS VPN
- 支持 UDP / HTTP / HTTPS / HTTP/3
- Compose 配置页 + 内存运行日志
- Release 签名与 R8 体积优化
